const Events = () => {
  return (
    <div
      className="w-full h-screen flex justify-center items-center bg-[#00ddff]"
      id="events"
    >
      Events
    </div>
  );
};

export default Events;
